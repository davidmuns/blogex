import { SortFileByTypePipe } from './sort-file-by-type.pipe';

describe('SortFileByTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SortFileByTypePipe();
    expect(pipe).toBeTruthy();
  });
});
